# ==============================================================================
# VARIABLES DE ENTORNO - Frontend Sistema Registro TxH
# ==============================================================================
#
# INSTRUCCIONES:
# 1. Copiar este archivo a .env.local en la misma carpeta:
#    En Windows: copy .env.example .env.local
#    En Linux/Mac: cp .env.example .env.local
#
# 2. Reemplazar los valores según tu entorno
#
# 3. NUNCA commitear .env.local al repositorio
#
# ==============================================================================

# ------------------------------------------------------------------------------
# API BACKEND
# ------------------------------------------------------------------------------

# URL del backend API (Node.js/Express)
# En desarrollo local
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# En producción, cambiar a:
# NEXT_PUBLIC_API_URL=https://api.txh-registro.uy/api

# ------------------------------------------------------------------------------
# CLERK AUTHENTICATION
# ------------------------------------------------------------------------------

# Clerk Publishable Key (del dashboard de Clerk)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...

# Clerk Secret Key (para middleware del servidor)
CLERK_SECRET_KEY=sk_test_...

# URLs de Clerk
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# ------------------------------------------------------------------------------
# APLICACIÓN
# ------------------------------------------------------------------------------

# Nombre de la aplicación
NEXT_PUBLIC_APP_NAME=Sistema Registro TxH

# Versión
NEXT_PUBLIC_APP_VERSION=1.0.0

# Entorno
NODE_ENV=development

# ------------------------------------------------------------------------------
# BRANDING (B2B - Personalización por institución)
# ------------------------------------------------------------------------------

# Nombre de la institución (se muestra en header, login, etc.)
# Si no se define, usa el nombre de la organización de Clerk
# NEXT_PUBLIC_INSTITUTION_NAME=Hospital Central de Montevideo

# URL del logo de la institución (PNG/SVG, mínimo 200x200px)
# Si no se define, usa el logo de la organización de Clerk
# NEXT_PUBLIC_INSTITUTION_LOGO_URL=https://hospital.com/logo.png

# Color primario de la marca (formato hex)
# Se usa para botones, links activos, acentos
# Default: #00a0a0 (verde quirúrgico)
NEXT_PUBLIC_PRIMARY_COLOR=#00a0a0
