# Test environment variables
NODE_ENV=test
PORT=4001

# Test database (PostgreSQL)
# CI/CD will override this with postgres:postgres@localhost
# For local testing, adjust user/password as needed
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/txh_registro_test?schema=public"

# JWT Secret for testing
JWT_SECRET="test-secret-key-for-testing-only-do-not-use-in-production"

# CORS
CORS_ORIGIN="http://localhost:3000"

# Logging
LOG_LEVEL="error"
