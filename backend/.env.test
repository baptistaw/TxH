# Test environment variables
NODE_ENV=test
PORT=4001

# Test database (PostgreSQL) - will be configured in CI/CD
# For local testing, ensure PostgreSQL is running and database exists
DATABASE_URL="postgresql://localhost:5432/txh_registro_test?schema=public"

# JWT Secret for testing
JWT_SECRET="test-secret-key-for-testing-only-do-not-use-in-production"

# CORS
CORS_ORIGIN="http://localhost:3000"

# Logging
LOG_LEVEL="error"
